# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.0.0

# Database
sqlalchemy>=2.0.0

# AI / LLM
groq>=0.11.0
google-genai>=1.0.0
sentence-transformers>=3.0.0

# Vector store
chromadb>=0.5.0

# Utilities
python-dotenv>=1.0.0
requests>=2.31.0

# Scraping (optional — only needed for sync/scrape feature)
beautifulsoup4>=4.12.0
lxml>=5.0.0
# playwright>=1.40.0  # Requires Chromium binary — not supported on Railway free tier
